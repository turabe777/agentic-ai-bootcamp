# Jupyter
jupyter>=1.1.1

# MCP Server dependencies
fastmcp>=3.1.1
mcp[cli]>=1.12.3

# LangGraph demo dependencies
# Pinned below 1.3: in langchain 1.3.x, create_agent(response_format=...) stops
# populating result["structured_response"], which breaks the Skills/SQL agent
# section of notebook 04 (verified 1.2.18 OK / 1.3.9 + 1.3.14 broken).
langchain>=1.2.12,<1.3
langchain-nvidia-ai-endpoints>=0.3.5
langgraph>=1.1.2

# NeMo Agent Toolkit
nvidia-nat[langchain]>=1.5.0
nvidia-nat[mcp]>=1.5.0
nvidia-nat[phoenix]>=1.5.0
nvidia-nat[eval]>=1.5.0
nvidia-nat[profiling]>=1.5.0

# Observability
arize-phoenix>=13.15.0
