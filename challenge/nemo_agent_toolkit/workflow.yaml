llms:
  nim_llm:
    _type: nim
    model_name: openai/gpt-oss-120b
    temperature: 0.0
    max_tokens: 1024
    base_url: https://integrate.api.nvidia.com/v1
    api_key: ${NVIDIA_API_KEY}

function_groups:
  ## TODO
  ## Please set the MCP URL to "http://localhost:${MCP_PORT:-8000}/mcp/"

workflow:
  ## TODO
